'use strict'

const html = require('../../lib/html')

function keyFileView (navigate) {
  const tree = html`<main>
    <h1>Create key file</h1>
  </main>`

  return tree
}

module.exports = keyFileView
